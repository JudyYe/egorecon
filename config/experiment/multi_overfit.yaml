# @package _global_

defaults:
  - default.yaml


general: 
  rerun: false
  wandb: false
  train_num_steps: 1_000_000
  vis_every: 1000  # vis_every = k* eval_every  but can be divisible by eval_every
  eval_every: 1000
  save_and_sample_every: 1000


datasets:
  t0: 300
  one_window: true
  split: mini
  test_split: mini