# @package _global_

# specify here default configuration
# order of defaults determines the order in which configs override each other
defaults:
  - _self_
  # - traindata
  # - testdata
  - paths: move.yaml
  - hydra: default.yaml
  - experiment: hoi
  - traindata: hotclip_train
  - testdata: hotclip_mini
  - eval: ddim_long


# This is just a placeholder to create the 'traindata' config node.
# It will be overridden by the actual dataset config.
# You can leave it empty or add a default target.


# task name, determines output directory path
expname: dev/tmp
exp_dir: ${paths.output_dir}/${expname}
ngpu: 1
log: wandb

# set False to skip model training
train: false

# evaluate on test set, using best model weights achieved during training
# lightning chooses best weights based on the metric specified in checkpoint callback
test: true
test_folder: eval

dir: null

# post_guidance: true
# inner_guidance: true
# # simply provide checkpoint path to resume training
# ckpt: ${exp_dir}/weights/${ckpt_index}
# ckpt_index: null

# # seed for random number generators in pytorch, numpy and python.random
# seed: null

# # sample: ddim_long
# # datasets:
# #   window: 150

# test_mode: guided
# legacy_mask: false
# vis_x0_list: false
# vis_x: true

# test_num: 100

